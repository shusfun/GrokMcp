# Grok 协作规则

- 凡是 Grok 能够完成的调查、分析、计划、实现、测试、审查或文档工作，优先并默认交给 Grok；Codex 不亲自执行可委派的工作。
- Codex 负责拆分任务、审查 Grok 的计划、决定是否批准，以及验收最终结果。只有 Grok 工具不可用、任务无法委派，或为完成审查与验收所必需时，Codex 才直接操作，并说明原因。
- 通过 Grok Supervisor 协作时，使用 `grok_wait` 等待 `plan_ready`、完成、失败或需要输入等边界状态，不读取或追踪过程流。
- Grok 断连或任务中断后，继续原来的 Grok session；不得创建替换会话规避恢复。
- Grok 使用的模型由用户配置；Codex 不修改模型、推理强度或其他模型设置。
- 不因委派而降低交付标准。Codex 必须核对 Grok 的计划是否覆盖需求、消费者、迁移与验证，并在验收失败时通过同一 session 要求返工。
